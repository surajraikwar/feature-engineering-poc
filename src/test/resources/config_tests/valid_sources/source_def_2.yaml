name: "customer_profiles_delta"
version: "2.1"
type: "delta"
entity: "customer"
location: "/data/customers/delta/v2.1" # Path to delta table
fields:
  - name: "customer_id"
    type: "string"
    required: true
  - name: "full_name"
    type: "string"
  - name: "email"
    type: "string"
config: # DatabricksSourceDetailConfig is used, but for path-based delta, location is key
  incremental: false
metadata:
  created_by: "test_user_2"
  tags: ["pii", "delta_table"]
